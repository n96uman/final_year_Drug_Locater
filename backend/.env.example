PORT=5000
MONGO_URI=mongodb://127.0.0.1:27017/drug_locator
JWT_SECRET=change_this_to_a_long_random_secret

# Chapa developer test payments:
# Get a test secret key from your Chapa dashboard and keep it private.
CHAPA_SECRET_KEY=CHASECK_TEST-xxxxxxxxxxxxxxxx
CHAPA_RETURN_URL=http://localhost:5173/payment/callback
PUBLIC_ORIGIN=http://localhost:5000

# Default admin (auto-seeded on first run if no admin exists):
#   Email:    admin
#   Password: finalyear
# Log in at the React app /login, then open /admin
